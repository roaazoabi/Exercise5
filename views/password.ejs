<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1>Please Set A Password To Your Account 🔐</h1>
    <form action="/password" method="POST">
      <div>
        <label id="lb">Create A New Password</label>
        <input type="password" name="password_1" required minlength="3" maxlength="32"></input>
      </div>
      <br>
      <div>
        <label id="lb">Retype The Password</label>
        <input type="password" name="password_2" required minlength="3" maxlength="32"></input>
      </div>
      <% if (locals.errorMessage) { %>
        <h5 id="error"><%= locals.errorMessage %></h5>
      <% } %>
      <br>
      <div>
        <button type="submit" id="login_details" name="done">Submit</button>
      </div>
    </form>
    <br>
    <div>
      <form action="/register" method="GET">
        <button type="submit">Go Back ⬅️</button>
      </form>
    </div>
  </body>
</html>